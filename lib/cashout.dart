import 'package:flutter/material.dart';

class CashOut extends StatelessWidget {
  const CashOut({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(padding: EdgeInsets.all(200),
      child: Column(children: [
        
      ],)),
    );
  }
}